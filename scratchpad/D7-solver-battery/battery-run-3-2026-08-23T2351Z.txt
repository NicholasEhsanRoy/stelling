THIS IS NOT A RE-DERIVATION OF THE PAGE'S TABLE, AND CANNOT BE ONE.
The harnesses behind docs/choosing-a-solver-backend.md's ten rows were never
committed and cannot be recovered from ten labels. Below are ten harnesses
BUILT TO those labels; run --rows to see, per row, exactly which parameters
the label left open and what this battery chose instead. The page's own cells
are printed beside ours and are never edited into agreement. Wall times are
this machine's, under this load. Read the direction, not the milliseconds.

ENVIRONMENT (the page's whole problem was a measurement with no provenance)
  when            : 2026-08-22 23:51 UTC
  stelling        : 0.2.0.dev0
  stelling tree   : /home/nick/MSF/stelling-wt/D7/src/stelling
  jax             : 0.11.0  (jax_enable_x64=True (set by this tool: the page's configuration))
  z3              : 5.0.0.0 (wheel)
  cvc5            : 1.3.4 (wheel)
  python          : 3.12.3
  platform        : Linux-6.8.0-138-generic-x86_64-with-glibc2.39  (x86_64)
  cpu             : 12th Gen Intel(R) Core(TM) i7-12850HX  x24
  load average    : 0.46 1.78 2.46   <- timings below are load-sensitive
  solver_timeout  : 10000 ms
  repeats per cell: 3

MECHANISM COLUMN (needs jax, needs NO solver, machine-independent)

 #  row                                page frag  measured   inputs  terms
------------------------------------------------------------------------------------
 1  scalar, linear                     QF_LRA     QF_LRA          1      4
 2  64-element array, linear           QF_LRA     QF_LRA         64    258
 3  8-element array, linear, false     QF_LRA     QF_LRA          8     34
 4  2 vars, degree 2 (AM–GM)           QF_NRA     QF_NRA          2      8
 5  2 vars, degree 6 (Motzkin)         QF_NRA     QF_NRA          2     14
 6  1 var, degree 3, false             QF_NRA     QF_NRA          1      4
 7  32 vars, 16 elementwise products   QF_NRA     QF_NRA         32     83
 8  64 vars, 32 elementwise products   QF_NRA     QF_NRA         64    163
 9  10-factor product chain            QF_NRA     QF_NRA         10     21
10  12-factor product chain            QF_NRA     QF_NRA         12     25

MEASURED HERE (this battery's harnesses, not the page's)

 #  row                                frag    both                   z3 alone               cvc5 alone            
--------------------------------------------------------------------------------------------------------------
 1  scalar, linear                     QF_LRA  unsat, 58–63 ms        unsat, 1–3 ms          unsat, 53–56 ms       
 2  64-element array, linear           QF_LRA  unsat, 65–73 ms        unsat, 3–4 ms          unsat, 62–64 ms       
 3  8-element array, linear, false     QF_LRA  sat, 61–66 ms          sat, 4–6 ms            sat, 56–58 ms         
 4  2 vars, degree 2 (AM–GM)           QF_NRA  unsat, 59–68 ms        unsat, 3–4 ms          unsat, 57–62 ms       
 5  2 vars, degree 6 (Motzkin)         QF_NRA  unsat, 63–72 ms        unsat, 5–6 ms          unsat, 56–65 ms       
 6  1 var, degree 3, false             QF_NRA  sat, 63–71 ms          sat, 2–3 ms            sat, 56–60 ms         
 7  32 vars, 16 elementwise products   QF_NRA  unsat, 19.7–19.9 s     unsat, 3.9–4.0 s       UNKNOWN, 16.0–16.0 s   ‡
 8  64 vars, 32 elementwise products   QF_NRA  UNKNOWN, 26.1–26.1 s   UNKNOWN, 10.1–10.1 s   UNKNOWN, 16.0 s        ‡
 9  10-factor product chain            QF_NRA  unsat, 2.2–2.3 s       unsat, 3 ms            unsat, 2.2–2.3 s      
10  12-factor product chain            QF_NRA  unsat, 16.0–16.0 s     unsat, 2 ms            UNKNOWN, 16.0–16.0 s  

THE PAGE'S CELLS, AS PUBLISHED (2026-08 hand-check, harnesses lost)

 #  row                                frag    both                   z3 alone               cvc5 alone             reconstruction 
--------------------------------------------------------------------------------------------------------------
 1  scalar, linear                     QF_LRA  unsat, 78–112 ms       unsat, 8–9 ms          unsat, 71–84 ms        reconstructed  
 2  64-element array, linear           QF_LRA  unsat, 86–91 ms        unsat, 10–12 ms        unsat, 77–87 ms        reconstructed  
 3  8-element array, linear, false     QF_LRA  sat, 86–90 ms          sat, 11–13 ms          sat, 75–117 ms         reconstructed  
 4  2 vars, degree 2 (AM–GM)           QF_NRA  unsat, 80–83 ms        unsat, 9 ms            unsat, 75–87 ms        reconstructed  
 5  2 vars, degree 6 (Motzkin)         QF_NRA  unsat, 92–106 ms       unsat, 12–13 ms        unsat, 81–83 ms        reconstructed  
 6  1 var, degree 3, false             QF_NRA  sat, 87–88 ms          sat, 11 ms             sat, 69–71 ms          reconstructed  
 7  32 vars, 16 elementwise products   QF_NRA  unsat, ~10.3 s         **UNKNOWN** (timeout)  unsat, 166–175 ms      unsupported    ‡
 8  64 vars, 32 elementwise products   QF_NRA  unsat, ~11.0 s         **UNKNOWN** (timeout)  unsat, 772–792 ms      unsupported    ‡
 9  10-factor product chain            QF_NRA  unsat, ~8.1 s          unsat, 123–133 ms      unsat, 8.3–8.5 s       direction only †
10  12-factor product chain            QF_NRA  unsat, ~16.7 s         unsat, 689–702 ms      **UNKNOWN** (timeout)  direction only 

‡ this row's label admits readings that disagree about the direction; see --rows and --variants.
† this row's own published cells are internally inconsistent; --rows says how.

  +N these cells invoked a backend N MORE times than stelling published a latency for, so their milliseconds
     are a lower bound: 10[both]+6, 10[z3]+3, 1[both]+6, 1[cvc5]+3, 1[z3]+3, 2[both]+6, 2[cvc5]+3, 2[z3]+3,
     4[both]+6, 4[cvc5]+3, 4[z3]+3, 5[both]+6, 5[cvc5]+3, 5[z3]+3, 7[both]+6, 7[z3]+3, 9[both]+6, 9[cvc5]+3,
     9[z3]+3
    invocations stelling made and published no latency for: the VERIFIED vacuity widen re-check (every
      discharged row), and the admitted-region check (only a slice carrying a relational assume). The cell's
      milliseconds are a LOWER BOUND on what the backends were asked to do

ALTERNATE READINGS OF THE CONTESTED ROWS

The spread IS the content. Each of these is a defensible reading of
the same row label; where they disagree, the label does not choose,
and no number from any of them can be filed against the page's row.

row  reading          frag    both                   z3 alone               cvc5 alone            
--------------------------------------------------------------------------------------------------------
  7  sum-of-squares   QF_NRA  unsat, 156–161 ms      unsat, 15–18 ms        unsat, 136–146 ms     
  7  cancellation     QF_NRA  unsat, 58–64 ms        unsat, 2–3 ms          unsat, 54–59 ms       
  8  sum-of-squares   QF_NRA  unsat, 1.1–1.1 s       unsat, 142–148 ms      unsat, 904–952 ms     
  8  cancellation     QF_NRA  unsat, 65 ms           unsat, 2–3 ms          unsat, 58–61 ms       
  9  one-variable     QF_NRA  unsat, 61–65 ms        unsat, 1–2 ms          unsat, 56–59 ms       
 10  one-variable     QF_NRA  unsat, 58–72 ms        unsat, 2–4 ms          unsat, 55–62 ms       

  row 7 [sum-of-squares]: sum(a^2 + b^2 - 2ab) >= 0 over [-1,1]^16 x [-1,1]^16
  row 7 [cancellation]: sum(a*b) - sum(b*a) >= 0 over [-1,1]^16 x [-1,1]^16
  row 8 [sum-of-squares]: sum(a^2 + b^2 - 2ab) >= 0 over [-1,1]^32 x [-1,1]^32
  row 8 [cancellation]: sum(a*b) - sum(b*a) >= 0 over [-1,1]^32 x [-1,1]^32
  row 9 [one-variable]: x^10 >= 0 over [-1, 2], the chain being one variable ten times
  row 10 [one-variable]: x^12 >= 0 over [-1, 2], the chain being one variable twelve times

CELLS THAT RAN PAST THEIR BUDGET, AND BY HOW MUCH
  solver_timeout_ms is the SOLVER's limit, not a wall. The cvc5 wheel runs in a wall-guarded child process at
  timeout*1.5 + 1s because its own tlimit does not reliably preempt the coverings solver.
  row 7 [both]: cvc5 (wheel) answered timeout after 16.0 s on a 10 s budget
  row 7 [cvc5 alone]: cvc5 (wheel) answered timeout after 16.0 s on a 10 s budget
  row 8 [both]: cvc5 (wheel) answered timeout after 16.0 s on a 10 s budget
  row 8 [cvc5 alone]: cvc5 (wheel) answered timeout after 16.0 s on a 10 s budget
  row 10 [both]: cvc5 (wheel) answered timeout after 16.0 s on a 10 s budget
  row 10 [cvc5 alone]: cvc5 (wheel) answered timeout after 16.0 s on a 10 s budget

FINDING 1 (QF_LRA: both backends decide everything): HELD — 3 of the 3 linear rows MEASURED on both backends were decided by both.
  z3 was faster than cvc5 on each of the 3 linear row(s) decided here, by 14.0x to 53.0x. THAT IS A TIMING, on one machine, at the load printed above, and part of it is a process spawn rather than solving — see the page. It moves between runs of this same tool by more than the seconds do.
FINDING 2 (QF_NRA: neither backend dominates): NOT DECIDABLE FROM THIS BATTERY.
  Not because it is false — because the two row labels it rests on admit readings that disagree about WHICH backend finishes, one of which runs the page's direction backwards. That is a property of the labels, not of this environment, so it does not change with what is installed.
  Rows 7, 8 carry a `contested` note: their labels admit readings that disagree with each other about the direction. Run --variants to see the spread. This tool refuses to file its numbers against those rows.
  row 9 (10-factor product chain), THIS battery's reading: z3 unsat 3 ms, cvc5 unsat 2.2–2.3 s.
  row 10 (12-factor product chain), THIS battery's reading: z3 unsat 2 ms, cvc5 UNKNOWN 16.0–16.0 s.
  row 7 read instead as [sum-of-squares]: z3 unsat 15–18 ms, cvc5 unsat 136–146 ms.
  row 7 read instead as [cancellation]: z3 unsat 2–3 ms, cvc5 unsat 54–59 ms.
  row 8 read instead as [sum-of-squares]: z3 unsat 142–148 ms, cvc5 unsat 904–952 ms.
  row 8 read instead as [cancellation]: z3 unsat 2–3 ms, cvc5 unsat 58–61 ms.
  row 9 read instead as [one-variable]: z3 unsat 1–2 ms, cvc5 unsat 56–59 ms.
  row 10 read instead as [one-variable]: z3 unsat 2–4 ms, cvc5 unsat 55–62 ms.
  Compare each of those with the same row above. Where a row's readings disagree about WHICH backend finishes, the label does not settle it and neither does this battery.
FINDING 3 (a full portfolio is not a full-portfolio answer): HELD — rows 7, 10 were decided with both backends installed and invoked, and the verdict disclosed PORTFOLIO DEGRADED because only one answered.

Wall times above are THIS MACHINE'S, at this load, on this build of each backend.
They are not a property of the solvers. The fragment column is machine-independent;
nothing else here is.
